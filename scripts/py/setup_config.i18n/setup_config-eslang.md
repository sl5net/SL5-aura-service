# DEFAULT_STRINGS['es'][0]
Región escrita: {país} | Vorschlag: {default_primary}

# DEFAULT_STRINGS['es'][1]
Sprachcode se utiliza o se utiliza para el modo terminal (Keine Sprachen).

# DEFAULT_STRINGS['es'][2]
Primäre Sprache (de, en… oder 'n' für nein, keine Sprache) - automatische Bestätigung in 8 Sekunden

# DEFAULT_STRINGS['es'][3]
Sekundäre Sprache (o 'none' predeterminado): mejora automática en 8 segundos

# DEFAULT_STRINGS['es'][4]
Drücken Sie die Eingabetaste zur Bestätigung oder geben Sie otro Sprachcode ein.

# DEFAULT_STRINGS['es'][0]
Región detectada: {país} | Sugerido: {default_primary}

# DEFAULT_STRINGS['es'][1]
Seleccione el idioma o escriba 'n' para Modo terminal (sin idiomas).

# DEFAULT_STRINGS['es'][2]
Idioma principal (de, en, etc. o 'n'): se confirma automáticamente en 8 segundos

# DEFAULT_STRINGS['es'][3]
Idioma secundario (o 'ninguno'): se confirma automáticamente en 8 segundos

# DEFAULT_STRINGS['es'][4]
Presione Entrar para confirmar o escriba un código de idioma diferente.

# secundario
ninguno

# secundario
ninguno

# excluye_cadena
todo