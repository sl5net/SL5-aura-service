# DEFAULT_STRINGS['de'][0]
Région concernée : {pays} | Choix : {default_primary}

# DEFAULT_STRINGS['de'][1]
Sprachcode wählen oder 'n' für Terminal-Modus (Keine Sprachen).

# DEFAULT_STRINGS['de'][2]
Primäre Sprache (de, en… oder 'n' für nein, keine Sprache) - Commande automatique en 8 secondes

# DEFAULT_STRINGS['de'][3]
Seconde étape (ou « aucun » par défaut) - Commande automatique en 8 secondes

# DEFAULT_STRINGS['de'][4]
Drücken Sie die Eingabetaste zur Bestätigung or geben Sie un anderen Sprachcode ein.

# DEFAULT_STRINGS['fr'][0]
Région détectée : {pays} | Suggéré : {default_primary}

# DEFAULT_STRINGS['fr'][1]
Sélectionnez la langue ou tapez « n » pour le mode terminal (pas de langue).

# DEFAULT_STRINGS['fr'][2]
Langage primaire (de, en, etc. ou 'n') - confirmation automatique en 8 s

# DEFAULT_STRINGS['fr'][3]
Langue secondaire (ou « aucune ») - confirmation automatique en 8 s

# DEFAULT_STRINGS['fr'][4]
Appuyez sur Entrée pour confirmer ou saisissez un code de langue différent.

# secondaire
aucun

# secondaire
aucun

# exclut_str
tous