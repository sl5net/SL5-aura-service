# المصدر ~/.zshrc

  # =========================================================================
        # تلقائي "تيلدا/" -> "~/" عند مفتاح الإدخال
  # =========================================================================
                                            قبول تجاوز الخط () {
                                               BUFFER="${BUFFER//tilde\//~/}"
                                                       zle .قبول الخط
                                                                            }
                        zle -N قبول تجاوز الخط قبول الخط

                                                      # المصدر ~/.zshrc