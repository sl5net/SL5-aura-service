# 源~/.zshrc

# =============================================================================
# 当按下 ENTER 键时自动“波浪号/”->“~/”
# =============================================================================
接受线路覆盖() {
BUFFER="${BUFFER//代号\//~/}"
zle .接受线
}
zle -N 接受行 接受行覆盖

# 源~/.zshrc