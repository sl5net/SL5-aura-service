# सीएलआई वर्कफ़्लो टूल्स इंस्टालेशन गाइड

पथ नेविगेटर प्लगइन में कुछ क्रियाएं अस्पष्ट खोज करने, फ़ाइलों को सूचीबद्ध करने और क्लिपबोर्ड में हेरफेर करने के लिए बाहरी कमांड-लाइन उपयोगिताओं पर निर्भर करती हैं। यदि ये उपकरण गायब हैं, तो आपको सिस्टम कंसोल में एक चेतावनी दिखाई देगी।

नीचे प्रत्येक समर्थित ऑपरेटिंग सिस्टम के लिए इंस्टॉलेशन निर्देश दिए गए हैं।

## आवश्यक उपयोगिताएँ

* **`fzf`**: सामान्य प्रयोजन कमांड-लाइन फ़ज़ी फ़ाइंडर।
* **`खोजें`** (या `एफडी`): मानक फ़ाइल-खोज उपयोगिता।
* **क्लिपबोर्ड टूल**: आउटपुट को सीधे आपके सिस्टम क्लिपबोर्ड पर पाइप करने के लिए उपयोग किया जाता है।
* **लिनक्स:** `xclip` (X11 वातावरण की आवश्यकता है)।
* **macOS:** `pbcopy` (पूर्व-स्थापित)।
* **विंडोज़:** `क्लिप` (पूर्व-स्थापित)।
* **`फ़ाइल`**: पूर्ण टर्मिनल पूर्वावलोकन के लिए फ़ाइल प्रकार निर्धारित करता है।

---

## स्थापना निर्देश

### 1. लिनक्स (आर्क / मंज़रो)
चूँकि आपका सिस्टम मंज़रो पर चलता है, आप `pacman` का उपयोग करके आवश्यक पैकेज स्थापित कर सकते हैं:

__CODE_ब्लॉक_0__



## 1. तेज़ फ़ाइल चयन (ऑरा कमांड)

`path_navigator` क्रिया निम्नलिखित Git-जागरूक `fzf` कमांड का उपयोग करती है। इसका उद्देश्य फ़ाइल पथ को सीधे सिस्टम क्लिपबोर्ड में आउटपुट करना है।

**कमांड तर्क:**
- Git रिपॉजिटरी के अंदर `git ls-files` का उपयोग करता है (अनदेखी फ़ाइलों को छोड़कर)।
- खोजने के लिए वापस गिरता है। -Git रिपॉजिटरी के बाहर f` टाइप करें।
- `xclip -selection clipboard` का उपयोग करके चयनित पथ को क्लिपबोर्ड पर आउटपुट करता है।

## 2. तेज़ फ़ाइल निष्पादन ('के' फ़ंक्शन)

लूप को पूरा करने के लिए, कस्टम शेल फ़ंक्शन `k` का उपयोग किया जाता है। यह फ़ंक्शन क्लिपबोर्ड से पथ लेता है और फ़ाइल को तुरंत `केट` में खोलता है।

### कार्यान्वयन

अपने शेल की कॉन्फ़िगरेशन फ़ाइल में निम्नलिखित फ़ंक्शन जोड़ें (उदाहरण के लिए, `~/.bashrc`, `~/.zshrc`):

__CODE_ब्लॉक_1__

### उपयोग

1. `path_navigator` कमांड का उपयोग करें (उदाहरण के लिए, अपने ट्रिगर टूल में `खोज फ़ाइल` टाइप करें)।
2. वांछित फ़ाइल ढूंढें और चुनें (उदाहरण के लिए, `src/main/config.py`)।
3. अपने टर्मिनल में, `k` टाइप करें और **ENTER** दबाएँ।
4. फ़ाइल तुरंत केट में खुलती है।
__CODE_ब्लॉक_2__