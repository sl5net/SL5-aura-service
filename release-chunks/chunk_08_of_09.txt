Fasse die Änderungen in diesem Git-Diff Abschnitt kurz auf Englisch zusammen.
Gruppiere nach: Neue Features, Bugfixes, Refactoring, Docs/Config.
Nur Stichpunkte, maximal 15 Punkte. Dies ist Abschnitt 8 von 9:

  In def main(logger, loaded_models, config, suspicious_events, recording_time, activ:
  In def main(logger, loaded_models, config, suspicious_events, recording_time, activ:
  In def main(logger, loaded_models, config, suspicious_events, recording_time, activ:
    +             if is_first_loading: # scripts/py/func/main.py in while True:
  In def main(logger, loaded_models, config, suspicious_events, recording_time, activ:
    +                 test_output_dir = tmp_dir / "sl5_aura" / "sl5_aura_self_test"
  In def main(logger, loaded_models, config, suspicious_events, recording_time, activ:
    +                 test_output_dir = tmp_dir / "sl5_aura" / "sl5_aura_self_test"
  In def main(logger, loaded_models, config, suspicious_events, recording_time, activ:
    +     finally:
    +         observer.stop()
    +         observer.join()
  In def main(logger, loaded_models, config, suspicious_events, recording_time, activ:

FILE: scripts/py/func/manage_audio_routing.py
  In def manage_audio_routing(mode, logger=None)::
    +         if logger:
    +             logger.error(f"Failed to manage audio routing on Linux: {e} | sink={sink_name}")

FILE: scripts/py/func/map_reloader.py
  In import sys:
  In import os:
  In from pyzipper import zipfile:
    + settings = DynamicSettings()
  In from .auto_fix_module import try_auto_fix_module:
  In LAST_MODIFIED_TIMES = {} # noqa: F824:
    + KNOWN_MAP_Names = {'FUZZY_MAP_pre', 'FUZZY_MAP', 'PUNCTUATION_MAP'}
  In def auto_reload_modified_maps(logger,run_mode_override)::
  In def auto_reload_modified_maps(logger,run_mode_override)::
    +     if getattr(settings, "DEV_MODE_memory", False):
  In def auto_reload_modified_maps(logger,run_mode_override)::
    +         log_memory_details("def start", logger)
  In def auto_reload_modified_maps(logger,run_mode_override)::
    +                 _reload_start_time = time.time()  # NEU
  In def auto_reload_modified_maps(logger,run_mode_override)::
    +                     gc.collect()
    +                     clear_regex_cache()
    +                     if log_all_map_reloaded:
    +                         logger.info(" Forced garbage collection before re-import.")
  In def auto_reload_modified_maps(logger,run_mode_override)::
  In def auto_reload_modified_maps(logger,run_mode_override)::
  In def auto_reload_modified_maps(logger,run_mode_override)::
  In def auto_reload_modified_maps(logger,run_mode_override)::
  In def auto_reload_modified_maps(logger,run_mode_override)::
    +                     importlib.reload(module_to_reload)
  In def auto_reload_modified_maps(logger,run_mode_override)::
    +                     if module_name is not None and hasattr(module_to_reload, 'on_reload') and callable(module_to_reload.on_reload):
  In def auto_reload_modified_maps(logger,run_mode_override)::
    +                     _reload_duration = time.time() - _reload_start_time  # NEU
    +                     if _reload_duration > 0.1:  # nur loggen wenn merklich langsam
    +                         logger.info(f"⌚ slow? 🐌 map reload: {module_name} took {_reload_duration:.2f}s")
    +                         if module_name not in KNOWN_MAP_Names:
    +                             logger.debug(f"lets do it conservative: if not {module_name} in KNOWN_MAP_Names and slo 🐌 ⏩ Skipping reload")
    +                             continue
  In def auto_reload_modified_maps(logger,run_mode_override)::
    +                         logger.info(f"✅ Successfully reloaded '{module_name} in  {_reload_duration:.2f}s'.")
  In def auto_reload_modified_maps(logger,run_mode_override)::
    +                     if module_to_reload is not None and hasattr(module_to_reload, 'FUZZY_MAP_pre'):
    +                         check_map_health(file_path=map_file_path, module=module_to_reload, logger=logger)
  In def auto_reload_modified_maps(logger,run_mode_override)::
    +                             if platform.system() == "Windows":
    +                                 windows_apply_correction_with_sync()
  In def auto_reload_modified_maps(logger,run_mode_override)::
  In def auto_reload_modified_maps(logger,run_mode_override)::
    +     if getattr(settings, "DEV_MODE_memory", False):
  In def auto_reload_modified_maps(logger,run_mode_override)::
    +         log_memory_details("def end", logger)
  In def _create_init_file(file_path: Path, logger)::
  In def _trigger_upstream_hooks(start_path: Path, project_root: Path, logger)::
    +     while stop_dir in current_dir.parents or current_dir == stop_dir:
  In def _trigger_upstream_hooks(start_path: Path, project_root: Path, logger)::
    +                             log4DEV('Example: ',logger)
  In config.maps.plugins.sandbox.de-DE.FUZZY_MAP_pre: name lauffe is not defined:
    +                 if module is not None and module and hasattr(module, 'on_folder_change') and callable(module.on_folder_change):
    +                     if (not start_path_current_dir
    +                             or not start_path_current_dir.name):
    +                         continue
    +                     if not start_path_current_dir.name.startswith('_'):
    +                         if log_everything:
    +                             logger.info(f"🛑 Skipping hook for '{start_path_current_dir.name}' - folder does not start with '_'")
    +                         continue
  In config.maps.plugins.sandbox.de-DE.FUZZY_MAP_pre: name lauffe is not defined:
    +                 logger.info(f'❌ 🚨 scripts/py/func/map_reloader.py:575 -> Exception: {e} <- …{str(file_path)[-45:]}')
  In config.maps.plugins.sandbox.de-DE.FUZZY_MAP_pre: name lauffe is not defined:
  In def zip_me_nopassword(zip_path_outer, current_dir_or_single_file)::
    +             for root, _, files in os.walk(target_path): # map_reloader.py:533
  In def zip_me_nopassword(zip_path_outer, current_dir_or_single_file)::

FILE: scripts/py/func/model_manager.py
  In import math:
    + settings = DynamicSettings()
  In SetLogLevel(-1):
  In from .notify import notify:
  In from .config.dynamic_settings import settings:
  In def manage_models(logger, loaded_models, desired_names, threshold_mb, script_dir:
    +     return None

FILE: scripts/py/func/normalize_punctuation.py
  In def normalize_punctuation(text: str, punctuation_map: dict, logger2) -> Tuple[st:
    +         log4DEV('📍 Map is empty, skipping.', logger2)
  In def normalize_punctuation(text: str, punctuation_map: dict, logger2) -> Tuple[st:
    +     if getattr(settings, "DEV_MODE_all_processing", False):

FILE: scripts/py/func/notify.py
  In from pathlib import Path:
    + settings = DynamicSettings()

FILE: scripts/py/func/password_extract.py
  In import time:
  In if not hasattr(sys, CACHE_ATTR_NAME)::

FILE: scripts/py/func/private_map_ex.py
  In import os:
  In def _private_map_unpack(map_file_key: str, logger) -> bool::
    +     map_dir = str(Path(map_file_key).parent)
  In def _private_map_unpack(map_file_key: str, logger) -> bool::
    +     file_key_name = Path(map_file_key).name
  In def _private_map_unpack(map_file_key: str, logger) -> bool::
    +             zip_name_base = Path(zip_file).stem
  In def _check_gitignore_for_security(logger) -> bool::
    +     gitignore_path = Path(__file__).parents[3] / ".gitignore"

FILE: scripts/py/func/process_text_in_background.py
  In import psutil:
  In from .auto_fix_module import try_auto_fix_module:
  In from .state_manager import should_trigger_startup:
  In from .state_manager import should_trigger_startup:
  In from .correct_text_by_languagetool import correct_text_by_languagetool:
  In from .notify import notify:
  In from .notify import notify:
  In from .setup_initial_model import get_model_name_from_key:
    + settings = DynamicSettings()
  In from .setup_initial_model import get_model_name_from_key:
    + global _active_window_title
  In GLOBAL_debug_skip_list=False:
    + def ensure_path(p: Union[str, os.PathLike, Path, None]) -> Optional[Path]:
    +     if p is None:
    +         return None
    +     return p if isinstance(p, (Path, os.PathLike)) else Path(p)
  In def load_module_from_path(script_path, run_mode_override=None)::
    +         f"d####### map_file_path={path.parent.parent.parent.parent.name} {path.parent.parent.parent.name} {path.parent.parent.name} {path.parent.name} {path.name} ++++++++++++++++++++++++")
  In if settings.ENABLE_AUTO_LANGUAGE_DETECTION::
    +         print("168: TODO: performance killer (2025-1225-1950)")
    +         print("168: TODO: performance killer (2025-1225-1950)")
    +         print("168: TODO: performance killer (2025-1225-1950)")
  In def is_plugin_enabled(hierarchical_key, plugins_config)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
    +     if getattr(settings, "DEV_MODE_memory", False):
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
    +         log_memory_details("def load_maps_for_language", logger)
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
    +     if getattr(settings, "DEV_MODE_memory", False):
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
    +         log_memory_details("next: auto_reload_modified_maps", logger)
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
    +     if getattr(settings, "DEV_MODE_memory", False):
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
    +         log_memory_details("last: auto_reload_modified_maps", logger)
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
    +         is_private = False
    +         if "._" in modname or "/_" in modname or "\\_" in modname  :
    +             is_private = True
    +             if settings.DEV_MODE_show_when_private_map_found:
    +                 log4DEV(f"is_private: 📚Found module candidate: {modname}",logger)
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
    +             injection_data = {
    +                 'source_modname': modname
    +             }
    +             if is_private:
    +                 injection_data['is_private'] = True
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
    +                 if is_private:
    +                     m = (f"⚠️ 💥 🚨 🛑 SECURITY WARNING: Found 'PUNCTUATION_MAP' in 🔑 private module '{modname}' "
    +                          f"Punctuation maps do NOT support privacy masking! "
    +                          f"Entries from this map will be logged in plain text. "
    +                          f"Please move sensitive rules to 'FUZZY_MAP'.")
    +                     logger.info(m)
    +                     logger.warning(m)
    +                     print(m)
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
    +                 if is_private:
    +                     for entry in module.FUZZY_MAP_pre:
    +                         if isinstance(entry, dict):
    +                             entry.update(injection_data)
    +                         elif isinstance(entry, (list, tuple)) and len(entry) > 0:
    +                             last_item = entry[-1]
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def load_maps_for_language(lang_code, logger, run_mode_override=None)::
  In def apply_fuzzy_replacement_logic(processed_text, replacement, threshold, logger:
  In def apply_fuzzy_replacement_logic(processed_text, replacement, threshold, logger:
  In def apply_all_rules_may_until_stable(processed_text, fuzzy_map_pre, logger)::
  In def apply_all_rules_may_until_stable(processed_text, fuzzy_map_pre, logger)::
  In def apply_all_rules_may_until_stable(processed_text, fuzzy_map_pre, logger)::
  In def apply_all_rules_may_until_stable(processed_text, fuzzy_map_pre, logger)::
  In def apply_all_rules_may_until_stable(processed_text, fuzzy_map_pre, logger)::
  In def apply_all_rules_may_until_stable(processed_text, fuzzy_map_pre, logger)::
  In def apply_all_rules_may_until_stable(processed_text, fuzzy_map_pre, logger)::
  In def apply_all_rules_may_until_stable(processed_text, fuzzy_map_pre, logger)::
  In def apply_all_rules_may_until_stable(processed_text, fuzzy_map_pre, logger)::
  In def apply_all_rules_may_until_stable(processed_text, fuzzy_map_pre, logger)::
  In def apply_all_rules_may_until_stable(processed_text, fuzzy_map_pre, logger)::
  In def apply_all_rules_may_until_stable(processed_text, fuzzy_map_pre, logger)::
  In def apply_all_rules_may_until_stable(processed_text, fuzzy_map_pre, logger)::
  In def apply_all_rules_may_until_stable(processed_text, fuzzy_map_pre, logger)::
  In def apply_all_rules_may_until_stable(processed_text, fuzzy_map_pre, logger)::
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def process_text_in_background(logger,:
  In def sanitize_transcription_start(raw_text: str) -> str::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def apply_all_rules_until_stable(text, rules_map, logger_instance)::
  In def clear_global_maps(logger)::
    ... (gekuerzt nach 40 Zeilen)

FILE: scripts/py/func/secure_packer_lib.py
  In import os:
  In import logging:
  In import zipfile:
  In from scripts.py.func.password_extract import _extract_password:
  In def execute_packing_logic(current_dir, logger)::
    +     log_everything = True
  In def execute_packing_logic(current_dir, logger)::
  In def execute_packing_logic(current_dir, logger)::
    +             logger.info(f"🎯scripts … secure_packer_lib.py : Target 📦 ZIP Path: {zip_path_outer}")
  In def execute_packing_logic(current_dir, logger)::
  In def execute_packing_logic(current_dir, logger)::
    +                     logger.info("⏱️ Timestamp Check:")
  In def execute_packing_logic(current_dir, logger)::
    +         file_key_name = Path(key_file).name
  In zip_created_at = {current_time}:

FILE: scripts/py/func/setup_initial_model.py
  In def main()::
    +         print(f"INFO: 'model_name.txt' {model_name_file} already exists -> Skipping create model_name.txt")

FILE: scripts/py/func/simple_plugin_cache.py
  In def _json_default_converter(obj)::
  In def get_cache_db_path(base_dir: Path) -> Path::
    +     return base_dir.parent / "data" / CACHE_DB_FILE
  In def get_cached_result(:
    +                 print("DEBUG: Cache entry found but timestamp is None. Treating as expired.")

FILE: scripts/py/func/start_languagetool_server.py
  In import importlib:
    + settings = DynamicSettings()
  In LT_ALREADY_RUNNING_SENTINEL = type(LTAlreadyRunning, (object,), {:
  In def _update_settings_file(logger, java_path)::
  In def _is_lt_server_responsive(url, timeout=1)::
    + def get_languagetool_jvm_args():
    +     cpu_cores = os.cpu_count() or 4
    +     ram_gb = psutil.virtual_memory().total / (1024 ** 3)
    +     threads = max(2, min(16, cpu_cores * 2))
    +     xms_gb = max(1, int(ram_gb * 0.25))
    +     xmx_gb = max(2, min(8, int(ram_gb * 0.50)))
    +     return {
    +         "xms": f"-Xms{xms_gb}G",
    +         "xmx": f"-Xmx{xmx_gb}G",
    +         "threads": str(threads),
    +     }
  In def start_languagetool_server(logger, languagetool_jar_path, base_url)::
  In def start_languagetool_server(logger, languagetool_jar_path, base_url)::
    +     full_base_url = f"http://127.0.0.1:{port}" # Using 127.0.0.1 explicitly to avoid Windows IPv6 localhost issues
  In def start_languagetool_server(logger, languagetool_jar_path, base_url)::
    +         args = get_languagetool_jvm_args()
    +         command_str = [
    +             java_executable_path,
    +             args["xms"],
    +             args["xmx"],
    +             "-jar", str(languagetool_jar_path),
    +             "--port", str(port),
    +             "--threads", args["threads"],
    +             "--address", '127.0.0.1',
    +             "--allow-origin", "*"
    +         ]
    +         log_dir = Path("log")
    +         log_dir.mkdir(exist_ok=True)
    +         log_file = open(log_dir / "languagetool_server.log", "w", encoding="utf-8")
  In def start_languagetool_server(logger, languagetool_jar_path, base_url)::
    +         languagetool_process = subprocess.Popen(command_str,
    +                                                 stdout=log_file,
    +                                                 stderr=log_file,
    +                                                 text=True,
    +                                                 encoding='utf-8', shell=False)
  In def start_languagetool_server(logger, languagetool_jar_path, base_url)::
    +         logger.fatal(f"x Failed to start LanguageTool Server process with shell=True: {e}")
  In def start_languagetool_server(logger, languagetool_jar_path, base_url)::
  In def start_languagetool_server(logger, languagetool_jar_path, base_url)::
    +     for _ in range(40):
    +         log_file.flush()
  In def start_languagetool_server(logger, languagetool_jar_path, base_url)::
    +             if stderr:
    +                 logger.error(f"LanguageTool STDERR:\n{stderr}")
  In def start_languagetool_server(logger, languagetool_jar_path, base_url)::
    +         time.sleep(1.0)

FILE: scripts/py/func/transcribe_audio_with_feedback.py
  In import webrtcvad # NEU: Import für Voice Activity Detection:
    + settings = DynamicSettings()
  In def _get_downsampled_data(raw_data, input_rate, logger)::
    +         logger.debug("Downsampling 48kHz Stereo to 16kHz Mono")
  In def get_device_id(device_setting, logger)::
    +         logger.info('transcribe_audio_with_feedback.py:110 get_device_id: device_setting is None')
  In def get_device_id(device_setting, logger)::
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
    +     last_toggle = 0
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
    +         logger.warning("AUTO_ENTER_AFTER_DICTATION_global is not defined in the global scope.")
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
    +     local_config_path = PROJECT_ROOT / "config/settings_local.py" # scripts/py/func/transcribe_audio_with_feedback.py:207
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
    +                     value_part = line.split("=")[1].strip()  # get all behin =
    +                     value_part = value_part.split("#")[0].strip()  # get all before #
    +                     initial_silence_timeout = float(value_part)
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
    +                     value_part = line.split("=")[1].strip()  # get all behin =
    +                     value_part = value_part.split("#")[0].strip()  # get all before #
    +                     SPEECH_PAUSE_TIMEOUT = float(value_part)
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
    +                     value_part = line.split("=")[1].strip()  # get all behin =
    +                     value_part = value_part.split("#")[0].strip()  # get all before #
    +                     value_without_whitespaces = str(value_part)
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
    +                         AUTO_ENTER_FLAG_FILE = Path("/tmp/sl5_aura/sl5_auto_enter.flag")
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
    +         with sd.RawInputStream(samplerate=input_rate, blocksize=10600,
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
    +                     listen_persistent_flag = (Path("C:/tmp") if platform.system() == "Windows" else Path(
    +                         "/tmp")) / "sl5_aura" / "aura_vosk_listen_persistent.flag"
    +                     is_listen_persistent = listen_persistent_flag.exists()
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
    +                         partial_result = json.loads(recognizer.PartialResult())
    +                         partial_text = partial_result.get('partial', '').lower()
    +                         suspend_flag = (Path("C:/tmp") if platform.system() == "Windows" else Path(
    +                             "/tmp")) / "sl5_aura" / "aura_vosk_suspended.flag"
    +                         is_suspended = suspend_flag.exists()
    +                         modus = 'toggle'
    +                         if modus == 'toggle':
    +                             if is_listen_persistent:
    +                                 if any(w in partial_text.lower() for w in
    +                                        ["einen"]):
    +                                     continue
    +                             if any(w in partial_text.lower() for w in ["kaktus", "kakturs", "klapptisch", "kette", "kräftig", 'teppich']):
    +                                 if time.time() - last_toggle > 2.0:  # 2 sec cooldown
    +                                     last_toggle = time.time()
    +                                     if is_suspended:
    +                                         suspend_flag.unlink(missing_ok=True)  # WAKE UP
    +                                         listen_persistent_flag.touch()
    +                                         logger.info("🚀 System ACTIVE (wake) — persistent listening enabled")
    +                                         speak_inclusive_fallback("Wake-Word", 'en-US')
    +                                     else:
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
  In def transcribe_audio_with_feedback(logger, recognizer, LT_LANGUAGE:
    ... (gekuerzt nach 40 Zeilen)

FILE: scripts/py/func/validate_map_structure.py
    + def check_map_health(file_path, module, logger):
    +     try:
    +         mtime_diff = time.time() - os.path.getmtime(file_path)
    +         if mtime_diff > 3600:
    +             return
    +     except Exception as e:
    +         print(f'2026-0301-1511 {e}')
    +         return
    +     map_entries = None
    +     for name in ['FUZZY_MAP_pre', 'FUZZY_MAP_post', 'GLOBAL_FUZZY_MAP_PRE']:
    +         if hasattr(module, name):
    +             map_entries = getattr(module, name)
    +             break
    +     if not map_entries or not isinstance(map_entries, list):
    +         return
    +     regex_indicators = r'[\^$|()\[\]\\]'
    +     try:
    +         with open(file_path, 'r', encoding='utf-8') as f:
    +             content = f.read()  # Die ganze Datei als ein String
    +     except Exception as e:
    +         print(f'2026-0301-1511 {e}')
    +         return
    +     file_changed = False
    +     for i, entry in enumerate(map_entries):
    +         if not isinstance(entry, (tuple, list)) or len(entry) < 2:
    +             continue
    +         item1 = str(entry[0])  # Sollte ID sein
    +         item2 = str(entry[1])  # Sollte Regex sein
    +         if bool(re.search(regex_indicators, item1)) and not bool(re.search(regex_indicators, item2)):
    +             if item1 in content and item2 in content:
    +                 pos1 = content.find(item1)
    +                 pos2 = content.find(item2)
    +                 if pos1 != -1 and pos2 != -1 and pos1 < pos2:
    +                     if (pos2 - pos1) < 200:
    +                         logger.warning(f"🛠️  Auto-Repair: Vertauschung in {os.path.basename(file_path)} gefunden!")
    +                         new_entry = list(entry)
    +                         new_entry[0], new_entry[1] = entry[1], entry[0]
    +                         map_entries[i] = tuple(new_entry)
    +                         placeholder = "###_AURA_SWAP_###"
    +                         content = content.replace(item1, placeholder).replace(item2, item1).replace(placeholder, item2)
    ... (gekuerzt nach 40 Zeilen)

FILE: scripts/py/func/windows_apply_correction_with_sync.py
    + windows_apply_correction_LAST_NOTIFY_TIME = 0
    + def windows_apply_correction_with_sync():
    +     windows_apply_correction_notify_cooldown = 3.0
    +     current_time = time.time()
    +     global windows_apply_correction_LAST_NOTIFY_TIME
    +     if (current_time - windows_apply_correction_LAST_NOTIFY_TIME) < windows_apply_correction_notify_cooldown:
    +         return
    +     ahk_path = shutil.which("AutoHotkey.exe")
    +     if not ahk_path:
    +         standard_paths = [
    +             r"C:\Program Files\AutoHotkey\AutoHotkey.exe",
    +             r"C:\Program Files\AutoHotkey\v1.1\AutoHotkey.exe",
    +             r"C:\Program Files\AutoHotkey\v2\AutoHotkey64.exe"
    +         ]
    +         for p in standard_paths:
    +             if os.path.exists(p):
    +                 ahk_path = p
    +                 break
    +     if not ahk_path:
    +         print("error: AutoHotkey not found!")
    +     PROJECT_ROOT = Path(__file__).resolve().parent.parent.parent.parent
    +     script_path = PROJECT_ROOT / "scripts" / "ahk" / "sync_editor.ahk"
    +     subprocess.run([ahk_path, script_path, "notify"])
    +     windows_apply_correction_LAST_NOTIFY_TIME = current_time

FILE: scripts/py/service_api.py
  In from fastapi.responses import JSONResponse:
  In from dotenv import load_dotenv:
    + def timestamp():
    +     return datetime.now().strftime("%Y%m%d_%H%M%S")
  In LT_URL = http://localhost:8082/v2/check:
  In async def process_text_command(request: ProcessRequest, valid: bool = Depends(ve:
    +             output_dir=TMP_DIR,
  In def process_text_cli(request: ProcessRequest, valid: bool = Depends(verify_api_k:
    +     unique_dir_name = f"cli_request_{timestamp()}"
    +     request_output_dir = TMP_DIR / "sl5_aura" / "tts_output" / unique_dir_name
  In def process_text_cli(request: ProcessRequest, valid: bool = Depends(verify_api_k:
    +         output_dir=TMP_DIR,

FILE: scripts/py/start_uvicorn_service.py
  In def find_and_kill_process_on_port(port)::
    + def ensure_dependencies():
    +     dependencies = ["uvicorn", "fastapi", "requests"] # Erweitere die Liste nach Bedarf
    +     for dep in dependencies:
    +         try:
    +             __import__(dep)
    +         except ImportError:
    +             print(f"--- Modul '{dep}' fehlt. Installiere jetzt... ---")
    +             subprocess.check_call([sys.executable, "-m", "pip", "install", dep])
  In def start_uvicorn_service(host, port, module_path)::
  In def start_uvicorn_service(host, port, module_path)::
    +             readme = "" # noqa: F841
    +             readme = """
  In def start_uvicorn_service(host, port, module_path)::
    +             readme = " "
    +             print(readme)
  In if __name__ == __main__::
    +     ensure_dependencies()

FILE: scripts/search_rules/search_rules.sh  [nur Name, kein Diff]

FILE: scripts/sh/type_watcher_keep_alive.sh  [nur Name, kein Diff]

FILE: setup/SystemCheck.ps1  [nur Name, kein Diff]

FILE: setup/UNINSTALL.txt  [nur Name, kein Diff]

FILE: setup/fix_permissions.bat  [nur Name, kein Diff]

FILE: setup/glogg_installer.bat  [nur Name, kein Diff]

FILE: setup/install_ahk_copyq.ps1  [nur Name, kein Diff]

FILE: setup/macos_setup.sh  [nur Name, kein Diff]

FILE: setup/manjaro_arch_setup.sh  [nur Name, kein Diff]

FILE: setup/suse_setup.sh  [nur Name, kein Diff]

FILE: setup/ubuntu_setup.sh  [nur Name, kein Diff]

FILE: setup/uninstall_ahk_copyq.bat  [nur Name, kein Diff]

FILE: setup/uninstall_ahk_copyq.ps1  [nur Name, kein Diff]

FILE: setup/windows11_setup.bat  [nur Name, kein Diff]

FILE: setup/windows11_setup.ps1  [nur Name, kein Diff]

FILE: setup/windows11_setup_with_ahk_copyq_fzf_glogg.bat  [nur Name, kein Diff]

FILE: start_aura.bat  [nur Name, kein Diff]

FILE: tools/X11_Connection_Hunter.sh  [nur Name, kein Diff]

FILE: tools/ai/create_peter_koans.py
    + REPO_ROOT = Path.cwd()
    + SOURCE_DIR = REPO_ROOT / "config" / "maps" / "koans_deutsch"
    + TARGET_DIR = REPO_ROOT / "config" / "maps" / "koans_2_peter_deutsch"
    + LANGUAGE = "de-DE"
    + HEADER = '''import re # noqa: F401
    + '''
    + def strip_schwierige_namen(content):
    +     """Entfernt den langen schwierigeNamen String – zu viel Rauschen für Peter."""
    +     content = re.sub(
    +         r'schwierigeNamen\s*=\s*""".*?"""',
    +         '',
    +         content,
    +         flags=re.DOTALL
    +     )
    +     return content
    + def extract_commented_rules(content):
    +     """Sammelt alle auskommentierten Regelzeilen."""
    +     pattern = re.compile(r"^\s*#\s*(\((['\"]).*?\2.*?\))\s*,?\s*$", re.MULTILINE)
    +     return pattern.findall(content)
    + def transform_map_file(content, fname, koan_name):
    +     """
    +     Transformiert eine Map-Datei für Peter:
    +     - Entfernt schwierigeNamen
    +     - Vereinfacht Header
    +     - Behält TODO + auskommentierte Regeln
    +     - Fügt klaren PETER-Hinweis hinzu
    +     """
    +     content = strip_schwierige_namen(content)
    +     commented = extract_commented_rules(content)
    +     map_var = "FUZZY_MAP_pre"
    +     if "FUZZY_MAP_pre" not in content:
    +         map_var = "FUZZY_MAP"
    +     peter_hint = f"# PETER-AUFGABE fuer Koan: {koan_name}\n"
    +     if len(commented) == 0:
    +         peter_hint += "# Keine auskommentierten Regeln gefunden.\n"
    +         peter_hint += "# -> Erstelle eine sinnvolle neue Regel fuer diesen Koan.\n"
    +     elif len(commented) == 1:
    +         peter_hint += "# Es gibt EINE auskommentierte Regel.\n"
    +         peter_hint += "# -> Entferne das '#' um sie zu aktivieren.\n"
    +         peter_hint += "# -> Was passiert danach in der Pipeline?\n"
    ... (gekuerzt nach 40 Zeilen)

FILE: tools/ai/peter.py
    + LOG_PATH = Path(__file__).parents[2] / "log" / "ai" / "peter.log"
    + logging.basicConfig(
    +     level=logging.DEBUG,
    +     format="%(asctime)s [%(levelname)s] %(message)s",
    +     handlers=[
    +         logging.FileHandler(LOG_PATH, encoding="utf-8"),
    +         logging.StreamHandler()  # auch auf Konsole
    +     ]
    + )
    + log = logging.getLogger("peter")
    + VERSION = "0.6.7"
    + def load_model_from_config():
    +     """Liest Modellname aus config/internal/ai_model.txt, fallback: llama3.2:latest"""
    +     for candidate in [
    +         Path(__file__).parents[2] / "config" / "internal" / "ai_model.txt",
    +     ]:
    +         if candidate.exists():
    +             model = candidate.read_text(encoding="utf-8").strip().splitlines()[0].strip()
    +             if model:
    +                 log.debug(f"Modell aus {candidate}: {model}")
    +                 return model
    +     return "llama3.2:latest"
    + MODEL_NAME = load_model_from_config()
    + OLLAMA_API_URL = "http://localhost:11434/api/generate"
    + SCRIPT_DIR = Path(__file__).parent
    + DB_PATH = REPO_ROOT / "data" / "peter_cache.db"
    + REPO_ROOT = Path(__file__).parents[2]
    + KOANS_ROOT = REPO_ROOT / "config" / "maps"
    + README_AI_PATH = REPO_ROOT / "README_AI.md"
    + LANGUAGE = "de-DE"
    + def init_db():
    +     LOG_PATH.parent.mkdir(parents=True, exist_ok=True)
    +     DB_PATH.parent.mkdir(parents=True, exist_ok=True)
    +     conn = sqlite3.connect(DB_PATH)
    +     conn.execute("""
    +         CREATE TABLE IF NOT EXISTS peter_sessions (
    +             id INTEGER PRIMARY KEY AUTOINCREMENT,
    +             koan_path TEXT,
    +             suggested_rule TEXT,
    +             mode TEXT,
    ... (gekuerzt nach 40 Zeilen)

FILE: tools/debug_lt_client.py
    + def test_lt_connection():
    +     url = "http://localhost:8082/v2/check"
    +     text = "der hausbau im wald ist nicht erlaubt"
    +     print(f"--- DEBUG: Testing LanguageTool Client ---")
    +     print(f"Target URL: {url}")
    +     print(f"Input Text: {text}")
    +     data = {
    +         'text': text,
    +         'language': 'de-DE',
    +         'enabledOnly': 'false'
    +     }
    +     try:
    +         response = requests.post(url, data=data, timeout=5)
    +         print(f"Status Code: {response.status_code}")
    +         if response.status_code == 200:
    +             result = response.json()
    +             matches = result.get('matches', [])
    +             print(f"Matches found: {len(matches)}")
    +             for match in matches:
    +                 print(f" - Rule: {match['rule']['id']}")
    +                 print(f" - Message: {match['message']}")
    +                 print(f" - Replacements: {[r['value'] for r in match['replacements']]}")
    +         else:
    +             print(f"ERROR: Server returned {response.text}")
    +     except requests.exceptions.ConnectionError:
    +         print("CRITICAL: Connection Refused. Python cannot reach localhost:8082.")
    +         print("Possibilities: Firewall blocking Python, or IPv4/IPv6 mismatch (localhost vs 127.0.0.1).")
    +     except Exception as e:
    +         print(f"EXCEPTION: {e}")
    + if __name__ == "__main__":
    +     test_lt_connection()

FILE: tools/del_all_self_test_zip_tmp_folders.py
    + readme = """
    + Erklärung / Hinweise
    +     Suche: Path.rglob(name) findet rekursiv alle Pfade mit genau diesem Namen. Wir filtern mit p.is_dir() und has_hidden_component() um versteckte Pfade auszuschließen.
    +     Löschen: shutil.rmtree() entfernt rekursiv ein Verzeichnis. Für symbolische Links prüfen wir is_symlink() und verwenden unlink() statt rmtree, damit der Link selbst entfernt wird, nicht das Ziel.
    +     Sicherheit: Standardmäßig fragt das Skript vor dem Löschen nach Bestätigung. Verwende --dry-run, um zuerst nur die Liste der Kandidaten zu sehen.
    +     Fehlerbehandlung: Ausnahmen beim Löschen werden gefangen und auf stderr ausgegeben, so dass ein fehlerhafter Pfad den Rest nicht abbricht.
    + """
    + lunuxcmds = """
    + Empfohlenes, sicheres Vorgehen (macOS / Linux)
    +     Trockenlauf — nur anzeigen, welche Ordner gelöscht würden (unbedingt zuerst ausführen):
    + find config/maps/plugins -type d -name 'self_test_zip_tmp' -print | grep -v '/\.'
    + find config/maps/plugins -type d -name 'self_test_zip_tmp' -print0 | while IFS= read -r -d '' d; do
    +   case "$d" in
    +   esac
    +   rm -rf -- "$d"
    + done
    + """
    + ROOT = Path("config/maps/plugins")
    + TARGET_NAME = "self_test_zip_tmp"
    + def has_hidden_component(p: Path) -> bool:
    +     return any(part.startswith('.') for part in p.parts)
    + def find_targets(root: Path, name: str):
    +     """Yield directories named `name` under `root`, skipping paths that contain hidden components."""
    +     if not root.exists():
    +         return
    +     for p in root.rglob(name):
    +         if p.is_dir() and not has_hidden_component(p):
    +             yield p
    + def remove_dir(path: Path, dry_run: bool):
    +     """Remove directory tree, special-case symlinks."""
    +     if dry_run:
    +         print("[DRY-RUN] Would remove:", path)
    +         return
    +     try:
    +         if path.is_symlink():
    +             print("Removing symlink:", path)
    +             path.unlink()
    +         else:
    +             print("Removing directory tree:", path)
    +             shutil.rmtree(path)
    ... (gekuerzt nach 40 Zeilen)

FILE: tools/download_all_packages.py
  In def main()::
  In def main()::
    +     is_ci = os.getenv('GITHUB_ACTIONS') == 'true'
    +     large_models = ['vosk-model-de-0.21', 'vosk-model-en-us-0.22']
    +     if is_ci:
    +         print("--> GitHub Actions detected. Auto-excluding large models to stabilize build.")
    +         for model in large_models:
    +             if model not in exclude_list:
    +                 exclude_list.append(model)

FILE: tools/export_to_copyq.py
  In from pathlib import Path:
  In def collect_examples_old_with_random_order()::
    + def ensure_even_backslashes(s: str) -> str:
    +     return re.sub(r'\\+', lambda m: m.group(0) if len(m.group(0)) % 2 == 0 else m.group(0) + '\\', s)
  In def export_to_copyq(items, tab_name)::
    +     except Exception as e202601222016:
    +         print(f"logger.info: Warning at {file}: {e202601222016}")
  In def export_to_copyq(items, tab_name)::
  In def export_to_copyq(items, tab_name)::
    +     try:
    +         proc = subprocess.run([copyq_exe, "tab", tab_name], check=True, capture_output=True, text=True, env=env)
    +         proc.check_returncode()
    +     except subprocess.CalledProcessError as e202601222017:
    +         print(f"logger.info: Warning at {file}: {e202601222017}")
  In def export_to_copyq(items, tab_name)::
    +         text = str(item['text']).replace('\\','\\\\') # important for windows
  In def export_to_copyq(items, tab_name)::
    +         p2 = ''
    +         if not tags:
    +             try:
    +                 p2 = Path(text).parents[1]
    +                 p2 = Path(*p2.parts[3:])
    +             except Exception as e202601231029 :
    +                 pass
    +             if p2:
    +                 tags.append(str(p2))

FILE: tools/fix_moved_links.py
    + DRY_RUN = True  # Auf False setzen, um Änderungen wirklich zu speichern!
    + def repair_i18n_links():
    +     files = glob.glob("**/*.i18n/*.md", recursive=True)
    +     link_pattern = re.compile(r'(\[!?.*?\])\((.*?)\)')
    +     mode_suffix = " (DEMO-MODUS - Keine Datei wird verändert)" if DRY_RUN else " (LIVE-MODUS - Dateien werden überschrieben!)"
    +     print(f"Starte Link-Reparatur{mode_suffix}")
    +     print(f"Gefundene Dateien: {len(files)}\n")
    +     total_fixes = 0
    +     for file_path in files:
    +         current_dir = os.path.dirname(file_path)
    +         file_fixes = 0
    +         new_lines = []
    +         try:
    +             with open(file_path, 'r', encoding='utf-8') as f:
    +                 lines = f.readlines()
    +         except Exception as e:
    +             print(f"Fehler beim Lesen von {file_path}: {e}")
    +             continue
    +         for line_num, line in enumerate(lines, 1):
    +             def replace_logic(match):
    +                 nonlocal file_fixes
    +                 prefix = match.group(1)
    +                 url = match.group(2)
    +                 if url.startswith(('http', '#', 'mailto:', '/')):
    +                     return match.group(0)
    +                 path_only = url.split('#')[0] if '#' in url else url
    +                 if not path_only: # Falls es nur ein Anker war (bereits oben abgefangen, aber sicher ist sicher)
    +                     return match.group(0)
    +                 potential_path = os.path.normpath(os.path.join(current_dir, "..", path_only))
    +                 if os.path.exists(potential_path):
    +                     file_fixes += 1
    +                     new_url = f"../{url}"
    +                     print(f"  [FIX] In {file_path} (Zeile {line_num}):")
    +                     print(f"        {url}  -->  {new_url}")
    +                     return f"{prefix}({new_url})"
    +                 else:
    +                     return match.group(0)
    +             new_line = link_pattern.sub(replace_logic, line)
    +             new_lines.append(new_line)
    +         if file_fixes > 0:
    ... (gekuerzt nach 40 Zeilen)

FILE: tools/generate_20_radio_segments.sh  [nur Name, kein Diff]

FILE: tools/git/git-brief.py
    + """
    +  Die "Quick & Dirty" Lösung (Git-Befehl)
    + Dieser Befehl ist oft schon ausreichend. Er zeigt die Statistik (welche Dateien) und dann den Diff ohne den Ballast von unveränderten Zeilen:
    + code Bash
    + git diff --stat && git diff -U0 --no-color
    +     -U0: Entfernt alle Kontextzeilen (du siehst nur die Zeilen mit + und -).
    +  "LLM-Diff" (Python-Skript)
    + Wenn du es wirklich elegant willst, nutzt du dieses Python-Skript. Es gruppiert die Änderungen nach Dateien und zeigt nur an, was hinzugefügt (+) oder entfernt (-) wurde, wobei es lange Blöcke kürzt.
    + """
    + def get_brief_diff():
    +     cmd = ["git", "diff", "-U0"]
    +     result = subprocess.run(cmd, capture_output=True, text=True)
    +     lines = result.stdout.splitlines()
    +     current_file = ""
    +     for line in lines:
    +         if line.startswith('+++ b/'):
    +             current_file = line[6:]
    +             print(f"\nFILE: {current_file}")
    +         elif line.startswith('@@'):
    +             parts = line.split('@@', 2)
    +             if len(parts) > 2 and parts[2].strip():
    +                 print(f"  In {parts[2].strip()}:")
    +         elif line.startswith('+') and not line.startswith('+++'):
    +             content = line[1:].strip()
    +             if content: print(f"    + {content}")
    +         elif line.startswith('-') and not line.startswith('---'):
    +             content = line[1:].strip()
    +             if content: print(f"    - {content}")
    + if __name__ == "__main__":
    +     get_brief_diff()

FILE: tools/keep-keys-up.sh  [nur Name, kein Diff]

FILE: tools/keep-keys-up_version2026-0302-1135.sh  [nur Name, kein Diff]

FILE: tools/map_tagger.py
  In import time:
  In except ImportError::
    + parser = argparse.ArgumentParser(description="Map tagger (non-interactive option)")
    + parser.add_argument("--yes", "-y", action="store_true", help="Automatically accept suggestions / skip prompts")
    + args = parser.parse_args()
    + if args.yes:
    +     SKIP_ALL_FAILURES = True  # oder separate FLAG_AUTOMATIC = True
  In def process_file(filepath)::
    +             if args.yes:
    +                 if success:
    +                     final_example = suggestion
    +                 else:
    +                     new_lines.append(line)
    +                     continue
  In def process_file(filepath)::
    +                 print(f"[{' | '.join(prompt_parts)}]")
    +                 user_input = input("> ").strip()
    +                 if user_input.lower() == 'q':
    +                     print("Beendet.")
    +                     sys.exit(0)
    +                 if user_input.lower() == 'sa':
    +                     SKIP_ALL_FAILURES = True
    +                     new_lines.append(line)
    +                     continue
    +                 if user_input.lower() == 's':
    +                     new_lines.append(line)
    +                     continue
    +                 final_example = ""
    +                 if user_input:
    +                     final_example = user_input
    +                 elif success:
    +                     final_example = suggestion
    +                 else:
    +                     new_lines.append(line)
    +                     continue

FILE: tools/move_translations.sh  [nur Name, kein Diff]

FILE: tools/ollama_speak_answers_only_espeak_loop.py
    + MODEL = "llama3.2"
    + VOICE = "de-de" # this is man
    + VOICE = "de+f2" # f3 means femail „de+f1“ und „de+f2“
    + SPEED = "150"
    + def speak(text):
    +     """Sendet Text an espeak."""
    +     if not text.strip():
    +         return
    +     subprocess.run(["espeak", "-v", VOICE, "-s", SPEED, text], stderr=subprocess.DEVNULL)
    + def main():
    +     print(f"--- Ollama Sprach-Chat aktiv ({MODEL}) ---")
    +     print("Tippe 'exit' zum Beenden.")
    +     context = []
    +     while True:
    +         try:
    +             user_input = input("\nDu: ")
    +             if user_input.lower() in ['exit', 'quit', 'beenden']:
    +                 break
    +             process = subprocess.Popen(
    +                 ["ollama", "run", MODEL, user_input],
    +                 stdout=subprocess.PIPE,
    +                 stderr=subprocess.PIPE,
    +                 text=True,
    +                 bufsize=1
    +             )
    +             print("Ollama: ", end="", flush=True)
    +             full_response = ""
    +             sentence_buffer = ""
    +             for line in process.stdout:
    +                 print(line, end="", flush=True)
    +                 full_response += line
    +                 sentence_buffer += line
    +                 if any(punct in line for punct in ['.', '!', '?']):
    +                     speak(sentence_buffer.strip())
    +                     sentence_buffer = ""
    +             if sentence_buffer.strip():
    +                 speak(sentence_buffer.strip())
    +             process.wait()
    +         except KeyboardInterrupt:
    +             print("\nBeendet.")
    ... (gekuerzt nach 40 Zeilen)

FILE: tools/search_rules.sh  [nur Name, kein Diff]

FILE: tools/start_killfirst_uvicorn_service.sh  [nur Name, kein Diff]

FILE: tools/tests/analyze_path_headroom.py
    + """
    +     Run the script: py
    + tools/tests/analyze_path_headroom.py
    +     Result: It will tell you exactly:
    +         Which file is the "culprit" (the longest one).
    +         How many characters your installation folder (e.g. C:\Users\...\Downloads\Project) currently uses.
    +         How many characters it is allowed to have max.
    + Example Output:
    + code Text
    + Longest internal file:   ...\models\vosk-model-de-0.21\rescore\G.fst
    + Internal path length:    45 characters
    + Maximum allowed length
    + for Project Root Folder: 214 characters
    + Current Root Path:       C:\Users\User\Downloads\Very_Long_Folder_Name\Project
    + Current Root Length:     230 characters
    + ❌ CRITICAL FAIL: The path is already too long by 16 characters!
    + """
    + MAX_PATH_LIMIT = 260
    + def analyze_path_headroom():
