# डॉक्स/डेवलपर_गाइड/ट्रिनो_इंटीग्रेशन.एमडी
इतिहास | कम  ✔
10093 डॉकर आरएम ट्रिनो 2>/dev/null; डॉकर रन -डी --नाम ट्रिनो -पी 8083:8080 ट्रिनोडब/ट्रिनो
10094 डॉकर लॉग ट्रिनो -एफ | grep -m1 "सर्वर प्रारंभ हुआ"
10095 स्रोत .venv/bin/सक्रिय करें
10096 पिप इंस्टाल ट्रिनो
10098 पिप शो ट्रिनो
10099 Python3 -c "\nimport trino\nconn = trino.dbapi.connect(host='localhost', port=8083, user='aura')\ncur = conn.cursor()\ncur.execute('SELECT 1')\nprint('Trino कनेक्शन:', cur.fetchone())\n"




वर्तमान:
config.json ──────► एबेने 2 (टर्मिनल)
└──► एबेने 3 (स्ट्रीमलाइट)
└──► एबेने 3.5 (वेब)
↑
सभी लेसन डीजलबे कॉन्फिग

विचार:

एबेने 2 (टर्मिनल) ─┐
एबेने 3 (स्ट्रीमलाइट) ─┼──► ट्रिनो ──► user_configs
एबेने 3.5 (वेब) ─┘ ├── टर्मिनल: EN अनुवाद = सत्य
├── वेब: डीई, केइन अनुवाद
└── प्रति उपयोगकर्ता: ईजीन ओवरराइड्स




config/
├── सेटिंग्स.py ← हाउप्ट-कॉन्फिग
├── सेटिंग्स_local.py ← लोकेल ओवरराइड्स
├── सेटिंग्स_local.py_Example.txt
├── सेटिंग्स_local.py_Example_user_...txt
└── फिल्टर/.बैकलॉक/फर्स्ट_रन/
└── सेटिंग्स_लोकल_लॉग_फ़िल्टर.py ← एक "संदर्भ-स्प्लिट"

+ विविध JSON-डेटियन और वर्शिडेनन ऑर्टन