# 终端模式（语言排除）

终端模式是没有为语音/文本处理单元安装或配置特定语言包的配置状态。

## 如何启用
在初始设置或语言选择脚本期间，当提示输入 **主要语言**时，请输入：
- `n`
- `无`
- `0`

## 效果
- **EXCLUDE_LANGUAGES** 设置为“全部”。
- 不会下载或初始化任何特定于语言的模型（如 Whisper 或 Vosk 模型）。
- 系统以“仅终端”模式运行，适用于低磁盘环境或仅需要核心 CLI 工具而无需本地化语音支持的情况。

## 环境变量
激活后，会生成以下导出：
__代码_块_0__