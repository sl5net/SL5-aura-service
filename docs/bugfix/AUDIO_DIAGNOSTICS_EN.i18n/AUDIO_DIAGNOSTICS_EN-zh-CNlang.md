# AUDIO_DIAGNOSTICS_EN.md

# Aura 的 Linux 音频诊断


### 1. 识别设备
列出 Python 环境中看到的所有音频设备：
__代码_块_0__
* **要查找的内容：** 注意麦克风的 **索引号** 和 **硬件 ID (hw:X,Y)**。

### 2. 谁在使用硬件？
如果您收到“设备繁忙”或“超时”错误，请检查当前哪个进程 (PID) 正在锁定音频硬件：
__代码_块_1__
* **提示：** 如果您看到“pipewire”或“wireplumber”，则表明声音服务器正在管理该设备。如果您直接在 PCM 设备上看到“python3”或“obs”PID，它们可能会阻止其他设备。

### 3.实时监控（PipeWire）
如果您的系统使用 PipeWire（现代 Manjaro 中的标准），那么这是进行实时诊断的最佳工具：
__代码_块_2__
* **要查找的内容：** 检查“ERR”列是否有丢失，并验证 Aura (16000Hz) 和 OBS (48000Hz) 不会导致 CPU 上的重采样过载。

### 4. 监听音频事件
当麦克风静音、取消静音或创建新流时查看实时更新：
__代码_块_3__
* **用法：** 运行此命令，然后启动 Aura。如果您立即看到许多“删除”事件，则说明进程正在崩溃或被拒绝。

### 5. 硬件旁路测试
测试您的麦克风是否在原始硬件级别上工作（绕过 PulseAudio/PipeWire）。录制 5 秒的音频：
__代码_块_4__
* **结果：** 如果此方法有效，但 Aura 无效，则问题出在声音服务器配置中，而不是硬件中。

### 6. 紧急重置
如果音频系统卡住：
__代码_块_5__


**提示更简单的工作流程：**

`/tmp/diagnose.txt && kate /tmp/diagnose.txt` 是 Befehl 的一个。 🌵🚀