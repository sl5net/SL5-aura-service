# توضيح السلوك الدقيق لسير عمل نظامك:
                                              اكس سبيس بريك اكس
                     ### شرح مصحح لسير العمل المتكامل

القاعدة الأولى لـ **تحويل الإدخال** و**التسمية** قبل تنفيذ إجراء البحث النهائي بواسطة القاعدة الثانية.

                                   #### 1. الإدخال: "was ist ein haus"

             #### 2. القاعدة 1: وضع العلامات/التحويل

```python
("was ist ein haus (Begriffsklärung)", r'^.*was ist ein haus$', 90,
 {'flags': re.IGNORECASE, 'skip_list': ['LanguageTool','fullMatchStop']})
```

* **الإجراء:** تمت مطابقة إدخال المستخدم `"was ist ein haus"` بنجاح.
* **النتيجة (داخلية):** يقوم النظام بإنشاء الإخراج/التسمية `"was ist ein haus (Begriffsklärung)"`.
* **متابعة:** بما أن `fullMatchStop` موجود في `skip_list`، فإن القاعدة المطابقة **DOES NOT STOP**. تستمر العملية حتى القاعدة التالية، التي تحمل المحتوى *المحول* أو *المسمى*.

           #### 3. القاعدة 2: الإجراء العام/التنفيذ

```python
('', r'(suche auf wikipedia nach|was sind|was ist|wer ist|wo ist|Wie groß ist)( ein| die| das| der)? (?P<search>.*)', 90, {
'flags': re.IGNORECASE,
'on_match_exec': [CONFIG_DIR / 'wikipedia_local.py']
})
```

* **الإجراء:** من المحتمل أن يطابق النظام الآن **النتيجة/التسمية الحالية** من الخطوة السابقة، وهي ``was ist ein haus (Begriffsklärung)\'' (أو يتطابق مع الإدخال الأصلي، ولكن البرنامج النصي الذي تم تنفيذه يعطي الأولوية للتسمية المحولة).
* **مطابقة البادئة:** لا تزال البادئة (`was ist`) متطابقة.
* **مجموعة الالتقاط:** تلتقط مجموعة الالتقاط `(?P<search>.*)` بقية السلسلة:
* إذا كان النظام يستخدم **إخراج القاعدة 1 كمدخل جديد**، فإنه يلتقط: **`haus (Begriffsklärung)`** (أو السلسلة المحولة بالكامل، والتي يتم بعد ذلك تحليلها بواسطة البرنامج النصي للتنفيذ).
* **التنفيذ:** تم تنفيذ البرنامج النصي `wikipedia_local.py`.

                                       #### 4. الإجراء النهائي:

* يتلقى البرنامج النصي `wikipedia_local.py` مصطلح/تصنيف البحث المعدل خصيصًا.
* يقوم البرنامج النصي بإجراء بحث في ويكيبيديا عن المصطلح المقصود: **`haus (Begriffsklärung)`**.

                                                              **خاتمة:**

يعد هذا الإعداد طريقة أنيقة للتعامل مع الاستعلامات الغامضة أو العامة. من خلال قيام القاعدة المحددة بتعديل الإدخال أو إنشاء تسمية مستهدفة ثم إجبار العملية على الاستمرار في قاعدة البحث العامة، فإنك تضمن تشغيل بحث ويكيبيديا ليس من أجل "haus" العام، ولكن من أجل الإدخال المحدد والموضح: **`haus (Begriffsklärung)`**.

وهذا يؤكد أن استبعاد `fullMatchStop` **ضروري** للسماح للقاعدة الأولى بالمعالجة المسبقة للاستعلام وإثرائه قبل أن يتم التصرف بناءً عليه بواسطة قاعدة التنفيذ للأغراض العامة.

                                           (sl5,4.12.\'25 12:24 الخميس)