# SL5 Aura 规则引擎文档

## 高级规则参数：覆盖安全检查

在某些情况下（例如，高度可靠的内部命令或简单、高置信度的输入），用户可能希望强制执行后处理步骤（如“fuzzyRules”），即使系统对初始语音识别的置信度较低。

默认情况下，SL5 Aura 采用安全护栏：如果输入变化较高 (LT_SKIP_RATIO_THRESHOLD)，则出于性能原因，将跳过后处理工具以防止不可靠的校正/幻觉。


要禁用特定规则的此安全检查，请将标识符添加到“skip_list”参数中：

__代码_块_0__