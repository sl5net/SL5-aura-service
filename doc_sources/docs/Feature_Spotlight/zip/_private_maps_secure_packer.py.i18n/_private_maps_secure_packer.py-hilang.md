ओएस आयात करें
उपप्रक्रिया आयात करें
लॉगिंग आयात करें
शटिल आयात करें
पाथलिब आयात पथ से

लकड़हारा = लॉगिंग.getLogger(__name__)

डीईएफ़ निष्पादित (डेटा):
उत्तीर्ण

def on_reload():
"""
एक 'मैत्रियोश्का-ज़िप' से पहले:
ऑर्डनर -> इनर.ज़िप (aura_secure.blob) -> पासवर्ड.ज़िप
वेरस्टेक्कट डाई कम्पलेट वेरज़ीच्निस्स्ट्रुक्टुर।
"""
logger.info("🔒 सिक्योरपैकर (मैत्रियोश्का): स्टार्टे सिचेरुंग...")

current_dir = पथ(__file__).parent
पेरेंट_डीआईआर = करंट_डीआईआर.पेरेंट

# ज़िप का नाम बताएं
zip_name_outer = current_dir.name.lstrip('_') + ".zip"
zip_path_outer =parent_dir/zip_name_outer

# 1. पासवर्ड सुचेन
key_file = अगला(parent_dir.glob("।*.py"), कोई नहीं)
यदि key_file नहीं है:
लकड़हारा.त्रुटि('❌ कुंजी-फ़ाइल fehlt!')
वापस करना

पासवर्ड = _extract_password(key_file)
यदि पासवर्ड नहीं है:
logger.error("❌ पासवर्ड नहीं मिलेगा!")
वापस करना

# 2. INNERES ज़िप erstellen (डेर "ब्लॉब")
# माता-पिता के समय के लिए एक वर्ष से अधिक समय तक एक श्रेइबज़ुग्रिफ़ को न्यूनतम अवधि के लिए ऑर्डर दें
temp_inner_zip =parent_dir / "aura_secure_temp" # .zip से पहले

कोशिश करना:
# आभा_सुरक्षित_temp.zip प्राप्त करें
श्यूटिल.मेक_आर्काइव(str(temp_inner_zip), 'ज़िप', str(current_dir))
temp_inner_zip_file =parent_dir / "aura_secure_temp.zip"

# उम्बेनेनेन इन डेन न्यूट्रलेन ब्लॉब-नामेन
blob_name = "aura_secure.blob"
ब्लॉब_पथ = पेरेंट_डीआईआर / ब्लॉब_नाम
श्यूटिल.मूव(str(temp_inner_zip_file), str(blob_path))

# 3. ÄUßERES ज़िप erstellen (वर्सचलुसेल्ट)
यदि सबप्रोसेस.कॉल ("कमांड -v ज़िप", शेल = ट्रू, stdout = सबप्रोसेस.DEVNULL) != 0:
logger.error("❌ 'zip' Befehl fehlt.")
वापस करना

# ज़िप में ब्लॉब पैक करें
सीएमडी = [
'ज़िप', '-जे', # -जे: जंक पाथ्स (किन पफेड स्पीचर्न, नूर डेटाइनेम)
'-पी', पासवर्ड,
str(zip_path_outer),
str(blob_path)
]

प्रक्रिया = सबप्रोसेस.रन(cmd, कैप्चर_आउटपुट=सही, टेक्स्ट=सही)

# औफ्राउमेन डेस ब्लॉब्स
ओएस.निकालें(blob_path)

यदि प्रक्रिया.रिटर्नकोड == 0:
logger.info(f"✅ सिक्योरपैकर: स्ट्रक्चर वर्स्टेक इन {zip_name_outer} gespeichert.")
अन्य:
logger.error(f"❌ ज़िप फ़हलर: {process.stderr}")

ई के रूप में अपवाद को छोड़कर:
logger.error(f"❌ पैक फेहलर: {e}")

def _extract_password(key_path):
कोशिश करना:
f के रूप में open(key_path, 'r', encoding='utf-8') के साथ:
f में पंक्ति के लिए:
यदि लाइन.स्ट्रिप().स्टार्ट्सविथ("#"):
साफ़ = लाइन.स्ट्रिप().एलस्ट्रिप('#').स्ट्रिप()
यदि साफ़ है: साफ़ लौटें
सिवाय: पास
वापसी कोई नहीं